name: build-deploy
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: [self-hosted, linux, X64]
    steps:
      - uses: actions/checkout@v2
      - name: build-container
        run: ls
  deploy:
    runs-on: [self-hosted, linux, X64]
    steps:
      - name: deploy
        run: pwd